# Active Plus — সম্পূর্ণ কোড অডিট রিপোর্ট

| | |
|---|---|
| **অডিটের তারিখ** | ১২ সেপ্টেম্বর ২০২৬ |
| **রিপোজিটরি** | `ajfrinch-ctrl/Active-plus` |
| **ব্রাঞ্চ / কমিট** | `arena/01a0961c-active-plus` @ `5985e9a` |
| **কোডবেস** | ৪টি HTML পেজ + ১২টি ES মডিউল + ৩টি CSS + service worker + manifest |
| **টেস্ট** | ১৬৪টি টেস্ট (১৬১ পাস, ৩ ফেল — আগে থেকেই ফেল করছে, বিবরণ L-1) |
| **পদ্ধতি** | ম্যানুয়াল কোড রিভিউ + স্ট্যাটিক grep স্ক্যান + jsdom-এ পারফরম্যান্স বেঞ্চমার্ক + WCAG কনট্রাস্ট হিসাব + টেস্ট রান |

> **TL;DR (English):** The UI/UX layer, Bengali localisation, XSS-safe rendering and the
> test suite are genuinely good. The problems are concentrated in the **data & trust layer**:
> the app is a browser-only database with no server-side authority, the Firebase "cloud mode"
> cannot work as shipped, several finance/exam code paths are unreachable or produce wrong
> numbers, and the localStorage store re-parses itself on every read (quadratic, OOM at ~2 000
> students). Nothing here is a rewrite — but items C-1…C-4 and H-1…H-9 must be fixed before
> this is used with real student data.

---

## সেভারিটি সংজ্ঞা

| লেভেল | অর্থ |
|---|---|
| 🔴 **Critical** | বাস্তব ডেটা নিরাপত্তা/অখণ্ডতা ঝুঁকি, অথবা ঘোষিত একটি মূল ফিচার একদমই কাজ করে না |
| 🟠 **High** | ভুল হিসাব/নীরব ডেটা ক্ষতি/গুরুতর পারফরম্যান্স সীমা — ব্যবহার শুরু করলেই ঘটবে |
| 🟡 **Medium** | ফাংশনাল গ্যাপ, অ্যাক্সেসিবিলিটি, রক্ষণাবেক্ষণযোগ্যতা — কাজ চলে কিন্তু ঋণ জমে |
| 🔵 **Low / Info** | ছোট ত্রুটি, পরিচ্ছন্নতা, ডকুমেন্টেশন |

## সারসংক্ষেপ

| সেভারিটি | সংখ্যা | আইডি |
|---|---|---|
| 🔴 Critical | ৪ | C-1 … C-4 |
| 🟠 High | ৯ | H-1 … H-9 |
| 🟡 Medium | ১৬ | M-1 … M-16 |
| 🔵 Low / Info | ৯ | L-1 … L-9 |
| ✅ ইতিবাচক | ১২ | P-1 … P-12 |

**এক নজরে সবচেয়ে জরুরি ৫টি:** C-1 (ক্লায়েন্ট-সাইড ট্রাস্ট বাউন্ডারি), C-4 (Firebase মোড অচল),
H-1 (বকেয়া তৈরির কোনো উপায় নেই), H-2 (আংশিক পেমেন্টে পুরো বকেয়া মুছে যায়), H-3/H-4 (নীরব ডেটা হারানো + quadratic পারফরম্যান্স)।

---

## সমাধানের অবস্থা (অডিটের পরের কমিট)

কোড-বাগ হিসেবে যা ঠিক করা সম্ভব ছিল সব ঠিক করা হয়েছে — নতুন ফিচার, নতুন UI বা ডিজাইন
পরিবর্তন ছাড়াই। টেস্ট স্যুট এখন **১৭৯/১৭৯ পাস** (আগে ছিল ১৬১/১৬৪), যার মধ্যে
`tests/bugfixes.test.mjs`-এ ১৫টি নতুন রিগ্রেশন টেস্ট।

### ✅ সমাধান করা হয়েছে

| আইডি | কী ঠিক হলো |
|---|---|
| C-4(৩) | `pushRemote` আর `.catch(() => null)` করে না — প্রত্যাখ্যাত লেখা এখন `sync-failed` হিসেবেই রিপোর্ট হয় |
| H-2 | পেমেন্ট আসল বকেয়ার বিপরীতে যাচাই হয়; আংশিক পেমেন্ট ব্যালেন্স কমায় (বকেয়া বন্ধ করে না); বেশি অঙ্ক প্রত্যাখ্যাত; একটিমাত্র payment রো সঠিক অঙ্ক/তারিখ/মাধ্যমসহ |
| H-3 | `storeSet`/`writeJSON`/`save()` সফলতা রিটার্ন করে; কোটা ফুললে `getDbStatus().persisted = false` ও ইউজারকে সতর্কবার্তা |
| H-4 | `load()` memoized (+ অন্য ট্যাবের লেখায় invalidate) → ১৫০০ শিক্ষার্থীতে `add()` ৫.২৪ms→**০.৪৪ms**, `analytics()` ৩২ms→**০.৩৪ms**; ২০০০-এ OOM আর হয় না |
| H-5(১,২) | খালি উত্তর আর নম্বর পায় না; `takeExam`-এ রিটেক ও বন্ধ-উইন্ডো গার্ড |
| H-6 | ব্যাকআপ রিস্টোর ফাইলে থাকা **সব** কালেকশন ফেরত দেয়; `DATA_VERSION` বদলালে ডেটা মুছে না গিয়ে মাইগ্রেট হয় |
| H-7 | ইউজার-দেওয়া প্রতিটি লিংক `safeUrl()` দিয়ে যাচাই — `javascript:`/`data:`/`vbscript:`/প্রোটোকল-রিলেটিভ ব্লক |
| H-8 | `monthlyCollection` = চলতি মাস; `pendingAssignments` = চেক-না-করা সাবমিশন; শূন্য-ভাগ গার্ড (`resultPercent`); "ঝুলন্ত অ্যাসাইনমেন্ট" কার্ডও ঠিক |
| H-9 | নতুন `sharedNotices()` — শিক্ষক/অ্যাডমিন বোর্ড ও "সর্বশেষ নোটিশ" থেকে ব্যক্তিগত পেমেন্ট রশিদ বাদ |
| M-1 | `guardianPhone` এখন অ্যাডমিন ফর্মে ধরা হয়; লেবেল ও ফলব্যাক ক্রম ঠিক (শিক্ষার্থী বনাম অভিভাবক) |
| M-3 | ফলাফলের লিডারবোর্ড বিদ্যমান অ্যাডমিন সুইচ (`homeCards.leaderboard`) দিয়েই চালু/বন্ধ করা যায় |
| M-6 | সিডের তারিখ আজকের সাপেক্ষে হিসাব হয় → ৩টি ফেল করা টেস্ট এখন পাস, আর কোনো দিন স্টেল হবে না |
| M-7 | মোডালে `role="dialog"`/`aria-modal`/`aria-labelledby`, ফোকাস ট্র্যাপ ও ফোকাস ফিরিয়ে দেওয়া |
| M-8 | নামবিহীন ফর্ম কনট্রোলে অ্যাক্সেসিবল নাম (শিক্ষক ফর্মের লেবেল, crud সার্চ, প্রশ্নব্যাংক, ফিল্টার, নম্বর-দানের ঘর) |
| M-10 | গ্লোবাল `error`/`unhandledrejection` গার্ড; `checkConnectionStatus()`-এর rejection পথ |
| M-12 | crud নম্বর ফিল্ড আর চুপচাপ `0` হয় না (অবৈধ হলে এরর); select অচেনা সংরক্ষিত মান হারায় না |
| M-14 (আংশিক) | প্রিক্যাশ থেকে হুবহু-একই ১ MB লোগোর ডুপ্লিকেট বাদ → ইনস্টল ওজন ৩.১০ MB → **২.০৪ MB** |
| L-3 | crud সার্চে debounce |
| L-4 | খালি মোবাইলে আর `'—'` সংরক্ষিত হয় না |
| L-8 | README আর দাবি করে না যে rules সার্ভার-সাইডে সীমানা মানে — প্রকৃত অবস্থা লেখা |
| অতিরিক্ত | `checkSubmission` এখন `checked: true` সেট করে — আগে নম্বর দেওয়ার পরও শিক্ষকের স্ক্রিনে "নম্বর দিন" ফর্ম থেকে যেত |

### ⏳ এখনো খোলা (আর্কিটেকচার/পণ্য-সিদ্ধান্ত দরকার, তাই এই কমিটে ছোঁয়া হয়নি)

- **C-1, C-2, C-3** — ক্লায়েন্ট-সাইড ট্রাস্ট বাউন্ডারি, ডেমো ক্রেডেনশিয়াল, হ্যান্ডঅফ টোকেন: সমাধানের জন্য সার্ভার-সাইড auth/ব্যাকএন্ড দরকার।
- **C-4(১,২,৪,৫)** — `roles/<uid>` বুটস্ট্র্যাপ, per-collection সিঙ্ক, সব পোর্টালে subscribe, last-write-wins: Firebase আর্কিটেকচার পুনর্গঠন ছাড়া সম্ভব নয়।
- **H-1** — বকেয়া তৈরি/মাসিক বিলিং: এটি নতুন ফিচার (নতুন UI লাগবে)।
- **M-2** (ডিসকাউন্ট ফিল্ড), **M-4** (অচল সেটিংস), **M-5** (মৃত `results` কালেকশন), **M-9** (কনট্রাস্ট — দৃশ্যমান পরিবর্তন), **M-11** (ভেক্টর PDF), **M-13** (ইনলাইন মডিউল ভাঙা, লিন্ট/CI/LICENSE), **M-15** (সেলফ-হোস্টেড ফন্ট), **M-16** (spec ডকুমেন্ট), **L-1, L-2, L-5, L-6, L-7, L-9**।

---

# 🔴 Critical

## C-1 — পুরো অ্যাপের বিশ্বাসযোগ্যতা ব্রাউজারে: auth, authz এবং সমস্ত ডেটা ক্লায়েন্ট-সাইড

**অবস্থান:** `js/store.js:19-27`, `js/auth.js:222-249` ও `402-419`, `js/data.js:1097-1126` (`PERMISSIONS`/`assertCan`), `js/data.js:166` (`load()`)

**সমস্যা:** সব ডেটা — শিক্ষার্থীর নাম, শ্রেণি, রোল, অভিভাবকের মোবাইল, ফি/বকেয়া, পরীক্ষার ফলাফল,
এবং **সব ইউজারের পাসওয়ার্ড হ্যাশ** — একটি `localStorage` কী-তে (`activeplus_data`, `js/data.js:16`) JSON আকারে বাস করে।
`requireRole()` (`auth.js:402`) এবং পারমিশন ম্যাট্রিক্স (`data.js:1097`) শুধু UI গেট: যেকেউ DevTools
কনসোলে `localStorage.setItem('activeplus_session', …)` লিখে অ্যাডমিন সেসন বানিয়ে নিতে পারে, অথবা সরাসরি
স্টোর এডিট করে নিজের ফি `পরিশোধিত` করে দিতে পারে।

**প্রভাব:**
- একই ডিভাইস/ব্রাউজার ব্যবহার করা যেকোনো ব্যক্তি সব শিক্ষার্থীর PII পড়তে, বদলাতে, এক্সপোর্ট করতে পারে।
- শিক্ষার্থী নিজের রেজাল্ট/বকেয়া নিজেই বদলে দিতে পারে — কোনো সার্ভার-সাইড যাচাই নেই।
- ক্লাসে/অফিসে শেয়ার্ড কম্পিউটার হলে এটি সরাসরি ডেটা ফাঁস।

**প্রস্তাব:**
1. ন্যূনতম: Firebase Authentication + Realtime Database-কে **একমাত্র সত্যের উৎস** বানান এবং rules ঠিক করুন (C-4, H-9 দেখুন)। ক্লায়েন্ট-সাইড চেকগুলো তখন UX-মাত্র হয়ে যাবে।
2. ব্যাকএন্ড না থাকলে অন্তত পাসওয়ার্ড হ্যাশ ও ইউজার টেবিল ব্রাউজারে রাখবেন না; লগিন একটি ছোট সার্ভিসে (Cloud Function / Supabase) সরান।
3. README-এর দাবি ঠিক করুন (L-8) — বর্তমানে এটি নিরাপত্তার ভুল ধারণা দেয়।

---

## C-2 — ডেমো অ্যাকাউন্টের পাসওয়ার্ড কোডে ও লগিন স্ক্রিনে ছাপানো; হ্যাশ দুর্বল; সেসনের মেয়াদ নেই

**অবস্থান:** `js/auth.js:39-66` (`DEMO_ACCOUNTS`), `js/auth.js:185` (সিড), `index.html:184-187` (স্ক্রিনে পাসওয়ার্ড প্রদর্শন + "ব্যবহার করুন" বাটন), `js/auth.js:98-103` (`hashPassword`), `js/auth.js:268` (সেসন লেখা), `js/auth.js:67-68`

**সমস্যা:**
- `admin@activeplus.edu / Admin@123`, `teacher@… / Teacher@123`, `student@… / Student@123` — প্লেইন-টেক্সটে সোর্সে আছে এবং লগিন পেজে দেখানো হয়; কোনো forced password change নেই।
- হ্যাশ = এক রাউন্ড SHA-256 (`crypto.subtle` না থাকলে FNV ফলব্যাক, `auth.js:85-96`) — কোনো iteration/slow KDF নেই; localStorage-এ রাখা হ্যাশ থেকে পাসওয়ার্ড বের করা তুচ্ছ।
- লগিন ব্যর্থতার কোনো কাউন্টার/রেট লিমিট/লকআউট নেই — অসীম ব্রুট-ফোর্স।
- URL handoff টোকেনের মেয়াদ ১২ ঘণ্টা (`TOKEN_TTL_MS`, `auth.js:68`), কিন্তু **localStorage-এর মূল সেসনের কোনো মেয়াদ নেই** — ব্রাউজার ডেটা না মুছলে সেসন চিরকাল সচল।

**প্রভাব:** প্রোডাকশনে নেওয়ার সঙ্গে সঙ্গেই যেকেউ অ্যাডমিন। শেয়ার্ড ডিভাইসে পূর্ববর্তী ইউজারের সেসন পরের জনের হাতে।

**প্রস্তাব:** ডেমো অ্যাকাউন্টগুলো `import.meta.env`/কনফিগ-ফ্ল্যাগ দিয়ে শুধু ডেমো মোডে রাখুন; প্রথম লগিনে পাসওয়ার্ড বদল বাধ্যতামূলক করুন; PBKDF2/scrypt (কমপক্ষে ১০০k iteration) ব্যবহার করুন; সেসনে `expiresAt` যোগ করে প্রতি বুটে যাচাই করুন; ৫ বার ব্যর্থ হলে ৫ মিনিট লক।

---

## C-3 — হ্যান্ডঅফ টোকেন "সাইন" করা নয় (কী ছাড়া হ্যাশ), আর টোকেন URL-এ যায়

**অবস্থান:** `js/auth.js:127-136` (`encodeSessionToken`), `js/auth.js:138-153` (`decodeSessionToken`), `js/auth.js:172-175` (`handoffUrl`), `js/auth.js:117-120` (`b64urlEncode`)

**সমস্যা:** ডক-কমেন্ট বলছে *"Signed, expiring copy of the session"*, কিন্তু স্বাক্ষরটি
`fallbackHash(json, 'activeplus-handoff')` — একটি **non-keyed** FNV-ধরনের হ্যাশ যার salt সোর্স কোডে পাবলিক।
অর্থাৎ এটি signature নয়; যেকেউ সোর্স পড়ে যেকোনো রোলের বৈধ টোকেন বানাতে পারে। টোকেনটি
`admin.html#s=<token>` আকারে URL-এ যায় → ব্রাউজার হিস্টরি, `Referer`, স্ক্রিনশেয়ার/লগে ছড়িয়ে পড়ে।
অতিরিক্তভাবে `b64urlEncode` ডিপ্রিকেটেড `unescape()` ব্যবহার করে।

**প্রভাব:** টোকেন পেলেই ওই ইউজারের সেসন (uid, role, name) হাতে চলে আসে এবং গ্রহণযোগ্য হিসেবে গৃহীত হয় (`adoptSessionFromLocation`, `auth.js:156`)।

**প্রস্তাব:** ক্লায়েন্টে "সাইনিং" সম্ভব নয় — এই দাবিটি কোড ও README থেকে সরান। টোকেন হ্যান্ডঅফের বদলে একই অরিজিনে `sessionStorage`/`localStorage` যথেষ্ট; তবু লাগলে সার্ভার-ইস্যুড শর্ট-লিভড টোকেন ব্যবহার করুন। `unescape()`-এর বদলে `TextEncoder`।

---

## C-4 — Firebase "ক্লাউড মোড" যেভাবে আছে তাতে কাজ করা অসম্ভব, আর ব্যর্থ সিঙ্ককে সাফল্য দেখায়

**অবস্থান:** `database.rules.json:5-8, 12-16, 23-24, 29`, `js/firebase.js:222-232` (`setUserRole`), `js/data.js:203-219` (`pushRemote`), `js/data.js:222` (`save`), `js/student-home.js:81` (একমাত্র `subscribeRemote` কল), `js/firebase.js:110-260` (per-collection refs)

**সমস্যা (৫টি আলাদা ত্রুটি):**

1. **কেউ admin নয়।** Rules প্রতিটি পাথে `root.child('roles/<uid>/role').val() === 'admin'` যাচায় (`rules.json:8, 15-16`), কিন্তু **কোথাও কোড `roles/<uid>` লেখে না** — `setUserRole()` শুধু localStorage-এ লেখে (`firebase.js:222`)। ফলে ডিপ্লয়ের পর প্রত্যেকের জন্য `activeplus/data` read/write **denied**।
2. **শিক্ষার্থী/শিক্ষক অচল।** ক্লায়েন্ট শুধু একক মিরর `activeplus/data` পড়ে/লেখায়; rules-এর per-collection পাথ (`students/fees/results/…`, `rules.json:23-71`) কোডে কখনো ব্যবহৃত হয় না। আর মিররটি admin-only → অ্যাডমিন ছাড়া সবাই অ্যাপই চালাতে পারবে না। `teacherClasses/<uid>/…` (`rules.json:29`) লেখার কোনো কোড নেই।
3. **ব্যর্থতা লুকানো হয়।** `pushRemote`-এ `r.set(payload).catch(() => null)` (`data.js:206`) → rejection **resolve** হয়ে যায় → `done()` চলে → `lastSync` সেট ও `error = null` (`data.js:212-213`)। অর্থাৎ Firebase লেখা প্রত্যাখ্যান করলেও UI দেখায় **"সিঙ্ক হয়েছে"**। এটি কোডের নিজের মন্তব্য *"report it honestly"* (`data.js:216`) এবং spec 51-এর সরাসরি লঙ্ঘন।
4. **লাইভ আপডেট একতরফা।** `subscribeRemote` শুধু `student-home.js:81`-এ যুক্ত; admin/teacher কখনো সাবস্ক্রাইব করে না → তাদের স্ক্রিনে অন্যের পরিবর্তন কখনো আসে না।
5. **Last-write-wins ডেটা ক্ষতি।** প্রতিবার পুরো স্টোর `set()` হয় → দুই অ্যাডমিন একসঙ্গে কাজ করলে একজনের সম্পূর্ণ পরিবর্তন মুছে যায় (কোনো merge/transaction নেই)।

**প্রভাব:** README যে বহু-ইউজার ক্লাউড মোডের প্রতিশ্রুতি দেয় তা সম্পূর্ণ অকার্যকর; worse, অ্যাডমিনকে মিথ্যা "synced" দেখিয়ে লোকাল ডেটা হারানোর ঝুঁকি তৈরি করে।

**প্রস্তাব:**
- সিঙ্ক আর্কিটেকচার বেছে নিন: (ক) per-collection path + সঠিক rules + `roles/<uid>` লেখার একটি নিরাপদ বুটস্ট্র্যাপ (Cloud Function), অথবা (খ) মিরর ছেড়ে Firestore-এ collection-ভিত্তিক ডকুমেন্ট + offline persistence।
- `pushRemote` থেকে `.catch(() => null)` সরিয়ে ব্যর্থতাকে `syncState.error`-এ তুলুন এবং UI-তে retry দিন; সফল সিঙ্কের পরেই `lastSync` বসান।
- `subscribeRemote` তিনটি পোর্টালেই যুক্ত করুন; লেখার ক্ষেত্রে per-node `update()` ব্যবহার করে clobbering কমান।
- যতদিন না এটি ঠিক হচ্ছে, README ও সেটিংস UI-তে ক্লাউড মোডকে "পরীক্ষামূলক/অসম্পূর্ণ" হিসেবে চিহ্নিত করুন।

---

# 🟠 High

## H-1 — বকেয়া/ফি তৈরির কোনো উপায়ই নেই: `db.fees.add` কখনো কল হয় না

**অবস্থান:** পুরো রিপোতে `db.fees.add(` = **০টি কল-সাইট** (grep যাচাইকৃত); সিডে মাত্র ৩টি `বকেয়া` রো (`js/data.js` SEED.fees); `settings.admissionFee = 500` — কোনো কনজিউমার নেই

**সমস্যা:** ফি কালেকশন মডিউল শুধু বিদ্যমান বকেয়া **আদায়** করতে পারে; নতুন বকেয়া **সৃষ্টি** করার কোনো UI, ফাংশন বা স্বয়ংক্রিয় মাসিক বিলিং নেই। ভর্তির ফি ইনভয়েস করারও ব্যবস্থা নেই।

**প্রভাব:** সিডের ৩টি বকেয়া পরিশোধিত হওয়ার পর "বকেয়া" তালিকা **চিরতরে খালি** — মাসিক আয়ের পুরো হিসাব বন্ধ। নতুন ভর্তি হওয়া শিক্ষার্থীর কোনো ফি রেকর্ডই তৈরি হয় না। এটি অ্যাপের কেন্দ্রীয় ব্যবসায়িক ফিচার অকার্যকর।

**প্রস্তাব:**
1. "বকেয়া তৈরি" UI (একক/বাল্ক: শ্রেণি-ব্যাচ-মাস অনুযায়ী) যোগ করুন → `db.fees.add({ id, studentId, month, amount, status: 'বকেয়া' })`।
2. মাসের শুরুতে স্বয়ংক্রিয় জেনারেশন (চালু করলে) + ডুপ্লিকেট গার্ড (`studentId+month` ইউনিক)।
3. ভর্তির সময় `admissionFee` ইনভয়েস করুন — না হলে সেটিংস থেকে ফিল্ডটি সরান (M-4)।

---

## H-2 — আংশিক পেমেন্টে পুরো বকেয়া "পরিশোধিত" হয়ে যায়; অ্যামাউন্ট যাচাই হয় না

**অবস্থান:** `js/data.js:506-530` (`receivePayment`), `admin.html:1331-1337` (ফর্ম প্রিফিল), `admin.html` ~1335-1400 (সাবমিট হ্যান্ডলার)

**সমস্যা:** ফ্লো-টি উল্টো এবং অরক্ষিত:
```
receivePayment(feeId, …)   // data.js:512 → status 'পরিশোধিত', payment.amount = fee.amount (পুরোটা)
…তারপর…  payment.update(id, { amount: <ফর্মের মান> })   // অ্যাডমিন যা টাইপ করেছে
```
- `fee.status` **শর্তহীনভাবে** `পরিশোধিত` হয় (`data.js:508` শুধু আগের স্ট্যাটাস দেখে)।
- ফর্মের `amount`-এর সঙ্গে বকেয়ার তুলনা **কোথাও হয় না**; `min="1"` ছাড়া কোনো ভ্যালিডেশন নেই (`admin.html:543`)।
- কোনো আংশিক পেমেন্ট, ওয়েভার, বা ডিসকাউন্ট মডেল নেই (payments-এ `paidAmount`/`dueRemaining` ফিল্ড নেই)।

**প্রভাব:** ৳১২০০ বকেয়ায় ৳৫০ নিলেও বকেয়া শূন্য দেখায়; রশিদে ভুল অঙ্ক; `totalDue` ও মাসিক আয় দুটোই মিথ্যা। আর্থিক অখণ্ডতার সরাসরি ক্ষতি।

**প্রস্তাব:** `receivePayment(feeId, { amount, method, date, receivedBy })` — এক কলে সব: (১) `amount > 0 && amount <= বাকি` যাচাই, (২) আংশিক হলে `fee.paid += amount` ও স্ট্যাটাস `আংশিক`, (৩) পূর্ণ হলে `পরিশোধিত`, (৪) একটিমাত্র payment রো সঠিক amount-সহ। UI-তে ফর্ম সাবমিটের **পরে** এটি কল করুন (বর্তমান দুই-ধাপ এনরিচমেন্ট বাদ দিন)।

---

## H-3 — `storeSet` কোটা-এরর গিলে ফেলে: ডেটা না লেখা হলেও "সফল" টোস্ট দেখায়

**অবস্থান:** `js/store.js:19-22` (`try { localStorage.setItem } catch (e) { /* blocked */ }`), `js/data.js:222` (`save()` কোনো রিটার্ন ভ্যালু নেই)

**সমস্যা:** লেখা ব্যর্থ হলে (QuotaExceededError, প্রাইভেট মোড, storage disabled) exception চুপচাপ খেয়ে ফেলা হয়;
`save()` কিছুই রিটার্ন করে না, তাই কলার জানতেই পারে না। সব মিউটেশন শেষে `showToast('…সংরক্ষিত হয়েছে', 'success')`।

**প্রভাব:** ~৫ MB সীমায় (Safari/মোবাইলে আরও কম) পৌঁছালে প্রতিটি নতুন রেকর্ড **চুপচাপ হারায়**; ইউজার রিলোড দিলে দেখে কাজটাই হয়নি। H-4 অনুযায়ী ~৪ ০০০–৮ ০০০ শিক্ষার্থীতেই এই সীমা আসে।

**প্রস্তাব:** `storeSet` → boolean/throw করুক; `save()` সফলতা রিটার্ন করুক; ব্যর্থ হলে লাল টোস্ট + "ব্যাকআপ নামান" প্রম্পট। কোটার ৮০%-এ পৌঁছালে আগেভাগে সতর্কতা (store size নিয়মিত মাপুন)।

---

## H-4 — স্টোয়ার লেয়ার quadratic: প্রতিটি কালেকশন অ্যাক্সেসে পুরো JSON রি-পার্স

**অবস্থান:** `js/data.js:166` (`load()` — কোনো memoization নেই), `makeCollection` (`js/data.js:290+`), `js/data.js:624` (`analytics()`)

**সমস্যা:** `db.students.list()`-এর মতো প্রতিটি কলে প্রতিবার `load()` → `JSON.parse(পুরো স্টোর)`।
একটি `db.students.add()` = ২টি parse + ২টি stringify। `analytics()` একবারে ১২টি কালেকশন পড়ে = ১২টি parse।

**বেঞ্চমার্ক (jsdom, এই রিপোতে চালানো):**

| শিক্ষার্থী | স্টোরের আকার | parse/রেকর্ড ইনসার্ট | analytics() |
|---|---|---|---|
| ২০০ | ~৭০ KB | ০.৮৫ ms | ~৫ ms |
| ৩০০ | ~১০৫ KB (৩৬২ KB রি-পার্স/কল) | ~১.৩ ms | ~৮ ms |
| ১ ৫০০ | ০.৫৩ MB | **৫.২৪ ms** | **৩২ ms** |
| ২ ০০০ | — | **Node OOM** | — |

অ্যাডমিন হোমের এক রেন্ডারে `analytics()` **২ বার** কল হয় (`admin-modules.js` renderOverview + স্ট্যাট কার্ড) → ১ ৫০০ শিক্ষার্থীতে প্রতি রেন্ডারে ~৬৪ ms শুধু পার্সিং-এ।

**প্রভাব:** প্রতিষ্ঠানের আসল আকারে (হাজার শিক্ষার্থী) ট্যাব খোলা, টাইপ করা, রিপোর্ট — সব সেকেন্ডজুড়ে আটকে যাবে; মোবাইলে ট্যাব ক্র্যাশ।

**প্রস্তাব:** `load()`-কে memoize করুন (একটি মডিউল-লেভেল ক্যাশ + `dirty` ফ্ল্যাগ; `save()`-এ invalidate)। কালেকশন-ভিত্তিক ইনডেক্স (Map by id) রাখুন। `analytics()`-এর ফলাফল প্রতি রেন্ডারে একবার হিসাব করে পুনর্ব্যবহার করুন। দীর্ঘমেয়াদে IndexedDB (H-3-এর কোটা সমস্যারও সমাধান)।

---

## H-5 — পরীক্ষা স্কোরিং ও গ্রেডিং-এ একাধিক ত্রুটি

**অবস্থান:** `js/data.js:547` (`scoreExam`), `js/exams.js:274-310` (`takeExam`/submit), `js/exams.js:290-292` (কোনো টাইমার নেই), `settings.negativeMarking`/`defaultExamDuration` (কোনো কনজিউমার নেই)

**সমস্যা:**
1. **উত্তর না দিলেও নম্বর।** `answers[qi] = formData.get('q'+qi)` → অনুপস্থিত হলে `null`; `scoreExam` করে `Number(answers[index]) === question.answer` → `Number(null) === 0`। ফলে সঠিক উত্তর অপশন ০ (অর্থাৎ A) হলে **খালি প্রশ্নও সঠিক** গণনা হয়। বর্তমানে HTML `required` (`exams.js:291`) এটি লুকিয়ে রাখে — কিন্তু যেকোনো প্রোগ্রাম্যাটিক কল/ভবিষ্যৎ পরিবর্তনে ফাঁস খুলে যাবে (latent)।
2. **রিটেক গার্ড শুধু UI-তে।** `takeExam` আবার `examResultFor(examId, studentId)` যাচায় না — বাটন লুকানোই একমাত্র বাধা। একই পরীক্ষার একাধিক `examResults` রো তৈরি করা যায় → লিডারবোর্ড/গড় বিকৃত।
3. **টাইমার নেই।** `exam.duration` দেখানো হয় কিন্তু কোনো countdown/enforcement নেই।
4. **negative marking অপ্রয়োগ।** সেটিংসে মান আছে, `scoreExam`-এ ব্যবহার নেই।
5. **উত্তরপত্র ক্লায়েন্টে।** `db.exams` localStorage-এ `answer` সহ থাকে — শিক্ষার্থী কনসোল থেকে সব সঠিক উত্তর পড়তে পারে (C-1-এর উপ-ফল)।

**প্রস্তাব:** `scoreExam`-এ `if (answers[i] === null || answers[i] === '') continue;` + `String(...)` তুলনা; নেগেটিভ মার্কিং প্রয়োগ; `takeExam`-এ সার্ভার/ডেটা-লেভেল রিটেক গার্ড (`examResultFor` থাকলে ব্লক); `duration` অনুযায়ী টাইমার + অটো-সাবমিট; স্কোরিং সার্ভারে না নেওয়া গেলে অন্তত প্রশ্ন ও উত্তর আলাদা কালেকশনে রাখুন যা শিক্ষার্থীর কাছে পৌঁছায় না।

---

## H-6 — ব্যাকআপ রিস্টোর ৫টি কালেকশন ফেলে দেয়; `DATA_VERSION` বদলালে সব ডেটা মুছে যায়

**অবস্থান:** `js/data.js:654-666` (`importBackup`, whitelist লাইন ৬৫৮), `js/data.js:168-169` (ভার্সন মিল না হলে রিসিড), `js/data.js:17`

**সমস্যা:**
- whitelist-এ ২০টি কালেকশন; SEED-এ আছে কিন্তু বাদ পড়েছে: **`tips`, `banners`, `materialProgress`, `studyActivity`, `challenge`**। ফলে "রিস্টোর সফল" দেখানোর পরেও টিপস/ব্যানার/অধ্যয়ন-অগ্রগতি/স্ট্রিক/ডেইলি চ্যালেঞ্জ হারায়।
- `DATA_VERSION` (বর্তমানে ৭) মিল না হলে `load()` **কোনো মাইগ্রেশন ছাড়াই** পুরোনো স্টোর ফেলে নতুন সিড বসায় (`data.js:168-169`)। অর্থাৎ কোড আপডেট করলেই ব্যবহারকারীর সব ডেটা নিঃশব্দে মুছে যেতে পারে।

**প্রভাব:** ব্যাকআপ/রিস্টোর একটি ভরসার ফিচার — এটি আংশিকভাবে মিথ্যা প্রতিশ্রুতি। ভার্সন বাম্প = ডেটা ধ্বংস।

**প্রস্তাব:** whitelist-এর বদলে "স্টোরে থাকা সব কী গ্রহণ করুন, অজানা কী আলাদাভাবে সংরক্ষণ করুন" নীতি; রিস্টোরের পর কোন কোন কালেকশন বাদ পড়ল তার সারসংক্ষেপ দেখান। `DATA_VERSION` মিসম্যাচে **মুছে ফেলার বদলে** মাইগ্রেশন ফাংশন (`migrate(old, fromVersion)`) চালান; মাইগ্রেশন না থাকলে পুরোনো ডেটা `activeplus/store.backup.<version>` কী-তে রেখে ইউজারকে জানান।

---

## H-7 — ম্যাটেরিয়াল/ব্যানার লিংকে `javascript:` URI গ্রহণযোগ্য; কোথাও CSP নেই

**অবস্থান:** শিক্ষক ম্যাটেরিয়াল ফর্ম `teacher.html:94` (`type="url"` শুধু HTML ভ্যালিডেশন, যা `novalidate`/কাস্টম ফ্লোতে এড়ানো যায়), ব্যানার/লিংক রেন্ডারার `href="${…}"`, কোনো `<meta http-equiv="Content-Security-Policy">` নেই (৪টি পেজেই যাচাইকৃত)

**সমস্যা:** লিংকের মান স্যানিটাইজ হয় না; `href="javascript:…"` ক্লিকে স্টোর্ড XSS চালায়। CSP না থাকায় সফল ইনজেকশনের কোনো সীমাবদ্ধতা নেই।

**প্রভাব:** একজন শিক্ষক/অ্যাডমিন অ্যাকাউন্ট থেকে সব ইউজারের ব্রাউজারে স্ক্রিপ্ট চালানো সম্ভব → স্টোর চুরি/পরিবর্তন (C-1-এর সঙ্গে মিলে পূর্ণ ডেটা দখল)।

**প্রস্তাব:** লিংক সেভের সময় প্রোটোকল হোয়াইটলিস্ট (`https:`, `http:`, `mailto:`, `tel:`) — বাকি সব প্রত্যাখ্যান; রেন্ডারে `escapeHtml` ছাড়াও `isSafeUrl()` হেল্পার। চারটি পেজেই কঠোর CSP যোগ করুন (`default-src 'self'`; ফন্ট/সিডএন আলাদাভাবে তালিকাভুক্ত; `unsafe-inline` কমানোর জন্য ইনলাইন মডিউলগুলো আলাদা ফাইলে সরান — M-13)।

---

## H-8 — `analytics()` ভুল সংখ্যা দেয় (মাসিক আয়, বাকি অ্যাসাইনমেন্ট), শূন্য-ভাগ গার্ড নেই

**অবস্থান:** `js/data.js:624-652`; লেবেল `js/admin-modules.js:443`

**সমস্যা:**
- `monthlyCollection: payments.reduce(সব)` (`data.js:642`) = **সর্বকালের** মোট, কিন্তু UI-তে দেখানো হয় **"মাসিক সংগ্রহ"** (`admin-modules.js:443`)। কোনো মাস ফিল্টার নেই।
- `pendingAssignments: assignments.filter(a => !a.checked)` (`data.js:639`) — `checked` ফিল্ড **কোথাও সেট হয় না** (শিক্ষক submissions মার্ক করেন, assignment নয়) → সংখ্যাটি সর্বদা মোট অ্যাসাইনমেন্টের সমান।
- লিডারবোর্ড/ক্লাস-পারফরম্যান্সে শূন্য দিয়ে ভাগ (`total === 0`) গার্ডবিহীন → NaN/অনন্ত শতাংশ।

**প্রভাব:** অ্যাডমিন ড্যাশবোর্ডের প্রধান KPI-গুলো মিথ্যা। আর্থিক সিদ্ধান্ত ভুল তথ্যের ভিত্তিতে হবে।

**প্রস্তাব:** `monthlyCollection`-কে চলতি মাসের (বা সেটিংস-নির্বাচিত মাসের) পেমেন্টে সীমিত করুন; `pendingAssignments`-এর সংজ্ঞা ঠিক করুন (যে submission-গুলো এখনো মার্ক করা হয়নি, বা প্রকাশিত অ্যাসাইনমেন্ট যেগুলোর ডেডলাইন বাকি); সব ভাগফলে `denominator ? … : 0`; প্রতিটি সংখ্যার পাশে তার সময়সীমা লিখে দিন (যেমন "এই মাসে")।

---

## H-9 — ব্যক্তিগত পেমেন্ট নোটিশ শিক্ষক/অ্যাডমিনের সাধারণ বোর্ডে ফাঁস হয়

**অবস্থান:** `js/data.js:522-529` (`forStudent: student.id` সহ নোটিশ), `teacher.html:521` (`db.notices.list().slice(0,50)`), `admin.html:792` ও `admin.html:1210`, `js/teacher-home.js:147`

**সমস্যা:** পেমেন্ট নিলে শিক্ষার্থী-নির্দিষ্ট নোটিশ ("পেমেন্ট গৃহীত: সেপ্টেম্বর — ৳১২০০") তৈরি হয়। শিক্ষার্থী পক্ষ `noticesFor()` দিয়ে ঠিকভাবে ফিল্টার করে, কিন্তু শিক্ষক/অ্যাডমিন বোর্ড **আনফিল্টারড** `db.notices.list()` ব্যবহার করে।

**প্রভাব:** একজন শিক্ষক সব শিক্ষার্থীর পেমেন্ট-স্ট্যাটাস ও অঙ্ক দেখে ফেলেন — গোপনীয় আর্থিক তথ্যের ফাঁস; নোটিশ বোর্ডও অপ্রাসঙ্গিক রো-তে ভরে যায়।

**প্রস্তাব:** একটি শেয়ার্ড `publicNotices()` / `noticesForAudience(role, studentId)` হেল্পার বানিয়ে সব বোর্ডে তা ব্যবহার করুন; `forStudent` সেট থাকা নোটিশ শুধু সেই শিক্ষার্থীর কাছে যাবে। (রোল-ভিত্তিক দৃশ্যমানতা H-1-এর ফি-ফিচারের সঙ্গে একই ফিল্টারে রাখা সহজ।)

---

# 🟡 Medium

## M-1 — `guardianPhone` কখনো সংগ্রহই হয় না, আর `phone` ফিল্ডের অর্থ ফর্ম ও সিডে আলাদা

**অবস্থান:** শিক্ষার্থী ফর্ম `admin.html:425-470`, রেকর্ড গঠন `admin.html:866-877`, সেটিংস টগল `admin.html:209`, প্রদর্শন `admin.html:917, 954, 1029`, `js/student-home.js:675`

**সমস্যা:** সিডে `phone` = শিক্ষার্থীর নিজের মোবাইল, `guardianPhone` = অভিভাবকের। কিন্তু অ্যাডমিন ফর্মে `phone`-এর লেবেল **"অভিভাবকের মোবাইল"** এবং ফর্মে `guardianPhone` ইনপুট **নেই** — সাবমিট রেকর্ডেও কী-টি নেই (`admin.html:866-877`)। ফলে নতুন ভর্তিতে অভিভাবকের নম্বর কখনো সংরক্ষিত হয় না; প্রোফাইল/ভর্তি-ফর্ম/WhatsApp সবাই `student.phone || student.guardianPhone` ফলব্যাকে চলে। উল্টোদিকে সেটিংসে "অভিভাবকের মোবাইল" (`edit_guardianPhone`) শিক্ষার্থীর নিজ-সম্পাদনার জন্য খোলা যায় — এমন ফিল্ড যা অ্যাডমিন নিজে দেখতে বা সেট করতে পারে না।

**প্রস্তাব:** ফর্মে আলাদা `guardianPhone` ইনপুট যোগ করুন; `phone`-এর লেবেল "শিক্ষার্থীর মোবাইল" করুন (বা দুটির অর্থ স্পষ্ট করে README-তে লিখুন); খালি থাকলে `'—'` না লিখে খালি স্ট্রিং রাখুন (L-4)।

## M-2 — "ছাড় রিপোর্ট" চিরতরে খালি: `discount` ফিল্ড কোথাও সেট হয় না

**অবস্থান:** `js/admin-modules.js:630-633` (`rows: () => … filter(s => Number(s.discount) > 0)`)

**সমস্যা:** কোনো ফর্মে, সিডে বা ইমপোর্টে `discount` নেই → ফিল্টার সর্বদা শূন্য রো দেয়। ১৭টি রিপোর্টের একটি অকার্যকর।

**প্রস্তাব:** শিক্ষার্থী ফর্মে "মাসিক ছাড় (৳)" ফিল্ড যোগ করুন (এবং H-2-এর ফি হিসাবে প্রয়োগ করুন), অথবা রিপোর্টটি তালিকা থেকে সরান।

## M-3 — ফলাফল-পেজের লিডারবোর্ড চালু করার কোনো UI নেই (কী-নাম মিল নেই)

**অবস্থান:** `js/student-home.js:806-807` (`db.settings.get().leaderboard`), অ্যাডমিন টগল `admin.html:242` (`home_leaderboard` → `homeCards.leaderboard`), সিডে অচল `leaderboardEnabled`

**সমস্যা:** কোড পড়ে `settings.leaderboard`, কিন্তু এই কী সিডে নেই এবং অ্যাডমিন UI-তেও নেই — শুধু `homeCards.leaderboard` আছে (যা হোমের র‍্যাঙ্ক টাইল নিয়ন্ত্রণ করে, `student-home.js:313, 412`)। টেস্টগুলোও কী-টি হাতে বসিয়ে দেয়, তাই বাগটি ধরা পড়ে না।

**প্রস্তাব:** একটি কী-তে একীভূত করুন (`leaderboard`) এবং অ্যাডমিন সেটিংসে টগল দিন; `leaderboardEnabled` মুছে ফেলুন।

## M-4 — ৮টি সেটিংস ফিল্ড সম্পূর্ণ অচল (কোনো UI নেই, কোনো কনজিউমার নেই)

**অবস্থান:** `js/data.js` SEED.settings

| ফিল্ড | অবস্থা |
|---|---|
| `admissionFee` (৫০০) | কোথাও পড়া হয় না; ভর্তি-ফি ইনভয়েস নেই (H-1) |
| `defaultExamDuration` | পরীক্ষায় প্রিফিল হয় না, টাইমার নেই (H-5) |
| `negativeMarking` | `scoreExam`-এ অপ্রয়োগ |
| `leaderboardEnabled` | অচল; কোড `leaderboard` পড়ে (M-3) |
| `autoPublishResult` | কোনো অটো-পাবলিশ লজিক নেই |
| `notificationsEnabled` | নোটিফিকেশন সর্বদা চালু |
| `academicYear`, `website` | কোনো রেন্ডারার/ডকে ব্যবহৃত হয় না |
| `passMark` | পড়া হয়, কিন্তু অ্যাডমিন UI-তে এডিট করা যায় না |

**প্রস্তাব:** প্রতিটি ফিল্ডকে হয় বাস্তব আচরণের সঙ্গে যুক্ত করুন, নয়তো সিড/ফর্ম থেকে সরিয়ে ফেলুন। "সেটিংস আছে কিন্তু কিছু করে না" — ইউজারের আস্থা নষ্ট করে।

## M-5 — `results` কালেকশন একটি মৃত সিড (৪ রো, `studentId` নেই, কোথাও পড়া হয় না)

**অবস্থান:** `js/data.js` SEED.results; আসল ফলাফল থাকে `examResults`-এ

**প্রস্তাব:** সিড থেকে সরান, `importBackup` whitelist (`data.js:658`) ও টাইপ/ডক থেকে ঝেড়ে ফেলুন — নইলে ভবিষ্যতে কেউ ভুল কালেকশনে লিখবে।

## M-6 — টেস্ট স্যুট লাল (৩টি ফেল) এবং কোনো CI নেই

**অবস্থান:** ব্যর্থ টেস্ট: `tests/data.test.mjs:18` ("exam window decides View vs Start…"), `tests/data.test.mjs:21` ("assignment submission and checking move through real states"), `tests/student-home.test.mjs:30` ("a student can submit a pending assignment from its details")

**সমস্যা:** সিডের তারিখগুলো হার্ডকোড করা বাংলা তারিখ (সেপ্টেম্বর ২০২৬); আজ ২০২৬-০৯-১২ হওয়ায় ডেডলাইন পার হয়ে গেছে → স্ট্যাটাস `pending`-এর বদলে `overdue`, সাবমিশন ব্লকড। এগুলো **আমাদের পরিবর্তনের আগে থেকেই** ফেল করছে (git stash করে যাচাইকৃত)। পাশাপাশি `.github/` নেই → টেস্ট কখনো স্বয়ংক্রিয়ভাবে চলে না।

**প্রস্তাব:** সিডের তারিখগুলো রিলেটিভ করুন (`todayBn()` থেকে ±দিন) অথবা টেস্টে স্থির "আজ" ইনজেক্ট করুন; GitHub Actions-এ `npm test` + একটি lint জব যোগ করুন; টেস্ট ফেল করলে সবুজ টিক না পাওয়া পর্যন্ত মার্জ বন্ধ রাখুন।

## M-7 — মোডালের কীবোর্ড/স্ক্রিন-রিডার আচরণ অসম্পূর্ণ

**অবস্থান:** `js/app.js:189-215` (`openModal`/`closeModal`/`initModals`)

**সমস্যা:** কোনো focus trap নেই (Tab করলে পেছনের ফর্মে চলে যায়), বন্ধ করার পর ফোকাস ট্রিগার বাটনে ফিরে যায় না, `aria-labelledby`/`role="dialog"` নেই, পেছনের কনটেন্ট `inert`/`aria-hidden` হয় না।

**প্রস্তাব:** `role="dialog" aria-modal="true" aria-labelledby="<শিরোনামের id>"` যোগ করুন; প্রথম/শেষ ফোকাসেবল উপাদানে Tab লুপ; খোলার আগে সক্রিয় উপাদান মনে রেখে বন্ধ করার পর তাতে ফোকাস ফেরত; সম্ভব হলে নেটিভ `<dialog>` API-তে সরে যান।

## M-8 — ২৫+ ফর্ম কনট্রোলের অ্যাক্সেসিবল নাম নেই / লেবেল যুক্ত নয়

**অবস্থান (যাচাইকৃত উদাহরণ):** `teacher.html:73-77, 89-94` (লেবেল আছে কিন্তু `for`/`id` নেই → প্রোগ্রাম্যাটিক সম্পর্ক নেই), `teacher.html:62-63` ও `js/crud.js:44` (শুধু placeholder), `admin.html:272` (qb-paste টেক্সটএরিয়া), `admin.html:277` ও `teacher.html:125` (ফাইল ইনপুট), `admin.html:343` (ফিল্টার), `teacher.html:590, 732-733` (ডাইনামিক রো)

**প্রস্তাব:** প্রতিটি ইনপুটে `id` + `<label for>` (বা কমপক্ষে `aria-label`); `crud.js`-এ সার্চ ইনপুটে `aria-label="খুঁজুন"` যোগ করুন; placeholder-কে কখনো লেবেলের বিকল্প ধরবেন না।

## M-9 — WCAG AA কনট্রাস্ট ব্যর্থ: প্রধান বাটন, ব্যাজ ও অ্যাকসেন্ট টেক্সট

**অবস্থান:** `css/style.css` (থিম ভেরিয়েবল) — পরিমাপকৃত অনুপাত:

| জোড় | অনুপাত | AA (৪.৫) |
|---|---|---|
| body text `#edf2f7` / `#2d3748` | ১০.৬৪ | ✅ |
| `--text-muted #a0aec0` / কার্ড সারফেস | ৪.৫৬ | ✅ (সীমারেখায়) |
| **`--accent #4299e1` / কার্ড সারফেস** | **৩.৩৭** | ❌ |
| **সাদা টেক্সট / অ্যাকসেন্ট বাটন `#4299e1`** | **৩.০৫** | ❌ |
| **`--error #e53e3e` / ব্যাকগ্রাউন্ড** | **২.৯১** | ❌ (AA-large-ও না) |
| সাদা / `btn-error` `#e53e3e` | ৪.১৩ | ❌ |
| `--success #38a169` / ব্যাকগ্রাউন্ড | ৩.৬৯ | ❌ |
| placeholder / ইনপুট | ৩.৩১ | ❌ |

**প্রস্তাব:** ডার্ক ব্যাকগ্রাউন্ডের জন্য উজ্জ্বল ভ্যারিয়েন্ট ব্যবহার করুন (যেমন accent `#63b3ed`, error `#fc8181`, success `#68d391`), বাটনের টেক্সট/ব্যাকগ্রাউন্ড জোড় ≥৪.৫:১ নিশ্চিত করুন; placeholder-এর opacity বাড়ান।

## M-10 — গ্লোবাল এরর হ্যান্ডলিং নেই; অরক্ষিত promise; ডিপ্রিকেটেড API

**অবস্থান:** কোথাও `window.onerror` / `unhandledrejection` নেই (grep যাচাইকৃত), `js/admin-modules.js:78` (`checkConnectionStatus().then(…)` — `.catch` নেই), `js/auth.js:118` (`unescape`), `service-worker.js:79-98` (`cache.put` promise `waitUntil`-এর বাইরে)

**প্রস্তাব:** একটি গ্লোবাল হ্যান্ডলার যোগ করুন যা ইউজারকে বাংলায় "অপ্রত্যাশিত সমস্যা — ডেটা সংরক্ষিত নাও হতে পারে" দেখায় ও কনসোলে স্ট্যাক রাখে; সব promise-এ `.catch`; SW-তে `event.waitUntil(cache.put(…))`।

## M-11 — PDF আসলে প্রতি পৃষ্ঠায় একটি JPEG ছবি (সার্চ করা যায় না, ভারী, প্রিন্টে ঝাপসা)

**অবস্থান:** `js/pdf.js:191-196` (`c.toDataURL('image/jpeg', 0.92)` → `addImage`)

**প্রভাব:** প্রতি পৃষ্ঠা ২০০–৪০০ KB; টেক্সট সিলেক্ট/সার্চ/কপি অসম্ভব; প্রিন্টারে ছবি ঝাপসা; আর্কাইভ করার উপযুক্ত নয়।

**প্রস্তাব:** ব্রাউজারের নেটিভ প্রিন্ট (HTML → "Save as PDF") ডিফল্ট রাখুন; ভেক্টর টেক্সট PDF লাগলে jsPDF-এর `html()`/`text()` পথ ব্যবহার করুন (ফন্ট সাবসেটসহ), ছবি শুধু ছবি-অংশে।

## M-12 — `crud.js` নম্বর ফিল্ড চুপচাপ ০ বানায়; অচেনা মান থাকলে সেলেক্ট প্রথম অপশনে বদলে যায়

**অবস্থান:** `js/crud.js:131` (`value = Number(value) || 0`), `js/crud.js:95` (`form.elements[f.name].value = row[f.name] ?? ''`), `js/crud.js:18-19` (অপশন তালিকা)

**সমস্যা:** খালি/অবৈধ নম্বর → `0` (ব্যবহারকারীকে জানানো হয় না)। এডিটের সময় সংরক্ষিত মান অপশন তালিকায় না থাকলে select-এর `.value` সেট হয় না → প্রথম অপশন দেখায় এবং সাভ করলে **পুরোনো মান বদলে যায়**।

**প্রস্তাব:** নম্বর ফিল্ডে `''` → `null` রাখুন এবং অবৈধ ইনপুটে এরর দেখান; select-এ সংরক্ষিত মান অপশনে না থাকলে একটি `<option selected>` হিসেবে যোগ করুন (বা ফিল্ডটি দূষিত না করে এরর দিন)।

## M-13 — রক্ষণাবেক্ষণ কাঠামো: বিশাল ইনলাইন মডিউল, ডুপ্লিকেট হেল্পার, লিন্ট/CI/লাইসেন্স নেই

**পরিমাপ:** `admin.html` = ১ ৬৬৮ লাইন, যার **৯৭৮ লাইন ইনলাইন `<script type="module">`**; `teacher.html` = ৯৯৯ (৬১৯ ইনলাইন); `index.html` = ২৮৯ (১৭১ ইনলাইন); `student.html` = ৯৫ (৩ ইনলাইন — ভালো প্যাটার্ন)। `bn()` হেল্পার **৮টি ফাইলে** আলাদাভাবে কপি করা। কোনো ESLint/Prettier কনফিগ, `.github/`, `LICENSE` নেই। টেস্টগুলো ইনলাইন স্ক্রিপ্ট চালানোর জন্য রিপো রুটে `.boot-*.mjs` টেম্প ফাইল লেখে — `.gitignore`-এ নেই।

**প্রভাব:** ইনলাইন মডিউল আলাদাভাবে লিন্ট/টেস্ট/ক্যাশ করা যায় না (টেস্টকে ফাইল এক্সট্র্যাক্ট করতে হয় — এটি নিজেই একটি smell); ছোট পরিবর্তনে ১ ০০০-লাইন ফাইল রিভিউ লাগে; লাইসেন্স না থাকলে পুনঃব্যবহারের আইনি অবস্থা অস্পষ্ট।

**প্রস্তাব:** `admin.html`/`teacher.html`-এর ইনলাইন কোড `js/admin-panel.js`/`js/teacher-panel.js`-এ সরান (`student.html` যেভাবে করেছে); `bn()`, `taka()`, `escapeHtml` একটি `js/format.js`-এ একত্রিত করুন; ESLint + Prettier + GitHub Actions; একটি LICENSE যোগ করুন; `.gitignore`-এ `.boot-*.mjs`।

## M-14 — প্রিক্যাশ ৩.১০ MB, তার ২.১৩ MB দুটি হুবহু একই লোগো; `icons:check` চলে না

**পরিমাপ:** `service-worker.js:13-44` PRECACHE = ৩০টি URL, মোট **৩.১০ MB**; `assets/logo.png` ও `assets/logo-transparent.png` — দুটোই ১২৫৪×১২৫৪, ১ ০৬৫ KB, **byte-identical** (md5 যাচাইকৃত); `manifest.json` লোগোকে ১২৫৪×১২৫৪ আইকন হিসেবে ঘোষণা করে; `tools/generate-icons.py:94` ৫১২×৫১২ আউটপুট তৈরি করার কথা বলে।
`npm run icons:check` → `ModuleNotFoundError: No module named 'PIL'` (Pillow কোথাও ডিক্লেয়ার করা নেই)।

**প্রভাব:** প্রথম ইনস্টলে অহেতুক ~২ MB ডাউনলোড (মোবাইল ডেটায় ব্যয়বহুল); আইকন টুলচেন ভাঙা; `npm run icons` চালালে কমিটেড ব্র্যান্ডিং ৫১২×৫১২-এ বদলে যাবে (জেনারেটরের সঙ্গে অ্যাসেট অসামঞ্জস্য)।

**প্রস্তাব:** একটি লোগো রাখুন (অথবা আসল ট্রান্সপারেন্ট ভ্যারিয়েন্ট তৈরি করুন), লোগোকে ৫১২×৫১৩-এ রিসাইজ/অপ্টিমাইজ করে প্রিক্যাশ থেকে ডুপ্লিকেট সরান; `tools/requirements.txt` (বা README-তে স্পষ্ট নির্দেশনা) যোগ করে Pillow পিন করুন; জেনারেটরের আউটপুট ও কমিটেড অ্যাসেট মিলিয়ে নিন।

## M-15 — অফলাইন-ফার্স্ট PWA-তে Google Fonts-এর বাহ্যি নির্ভরতা

**অবস্থান:** চারটি পেজেই `<link … fonts.googleapis.com/fonts.gstatic.com>` (`display=swap`)

**প্রভাব:** ইন্টারনেট না থাকলে ফন্ট পড়ে → বাংলা টাইপোগ্রাফি ভেঙে যায় (পেজ ফাংশনাল থাকলেও); প্রিক্যাশে ফন্ট নেই; তৃতীয় পক্ষের অনুরোধ (গোপনীয়তা)।

**প্রস্তাব:** ফন্ট সাবসেট সেলফ-হোস্ট করে প্রিক্যাশে যোগ করুন, এবং `font-family`-তে শক্তিশালী লোকাল ফলব্যাক (`'Noto Sans Bengali', 'SolaimanLipi', 'Kalpurush', system-ui`) রাখুন।

## M-16 — কোডে ২৯টি "spec NN" রেফারেন্স, কিন্তু স্পেক ডকুমেন্ট রিপোতে নেই

**পরিমাপ:** `grep -E "[Ss]pec(ification)? ?[0-9]+"` → **২৯টি**, কমপক্ষে ১৩টি ভিন্ন নম্বর (spec 6, 8, 29, 40, 43, 46, 51, 57, 60, 62 …); কোথাও `docs/` বা স্পেক ফাইল নেই। পাশাপাশি একটি কমেন্টে "global search" উল্লেখ আছে যা বাস্তবায়িত হয়নি।

**প্রভাব:** "spec 51" / "spec 62"-এর মতো গুরুত্বপূর্ণ নিয়ম যাচাই করার কোনো উপায় নেই — রিভিউয়ার ও নতুন অবদানকারী অনুমানের ওপর নির্ভর করেন (H-4-এর `.catch(() => null)` ঠিক এই ফাঁকেই spec 51 লঙ্ঘন করেছে)।

**প্রস্তাব:** `docs/SPEC.md`-তে নম্বরভিত্তিক প্রয়োজনীয়তার তালিকা যোগ করুন (প্রতিটি spec-এর এক লাইন সংজ্ঞা), অথবা কমেন্টগুলো স্বয়ংসম্পূর্ণ করুন ("never report a saved record that could not be saved" যেমন ইতিমধ্যে আছে — সেটাই ভালো প্যাটার্ন)।

---

# 🔵 Low / Info

**L-1 — টেস্ট কভারেজের ফাঁক।** ১৬৪ টেস্ট আছে (ভালো), কিন্তু এই ফাংশনগুলোর জন্য শূন্য কভারেজ: `previewDocument`, `canvasesToPdf`, `renderIdCardCanvas`, `renderLedgerCanvases`, `nextTeacherId`, `changePassword`, `updateProfile`। → ডকুমেন্ট/PDF পাইপলাইন ও অ্যাকাউন্ট অপারেশনের জন্য টেস্ট যোগ করুন।

**L-2 — শিক্ষার্থীর ছবি শুধু URL (আপলোড নেই)।** `admin.html` photo ফিল্ড `https://…` চায়; ভাঙা/হটলিংক করা ছবির কোনো ফলব্যাক ভ্যালিডেশন নেই। → অন্তত `onerror` প্লেসহোল্ডার + প্রিভিউ; দীর্ঘমেয়াদে ফাইল আপলোড (base64 নয় — কোটা দেখুন H-3)।

**L-3 — সার্চে debounce নেই।** `crud.js` প্রতি কীস্ট্রোকে ফিল্টার + রি-রেন্ডার করে; H-4-এর রি-পার্সের সঙ্গে গুণ হয়ে ল্যাগ বাড়ে। → ১৫০–২৫০ ms debounce।

**L-4 — খালি মোবাইলে আক্ষরিক `'—'` সংরক্ষিত হয়।** `admin.html:876` (`String(data.get('phone') || '—')`) → ডেটাফিল্ডে ড্যাশ; CSV/রিপোর্টে অশুদ্ধ। `waNumber()` এটি সামলায়, তবু ডেটা-কোয়ালিটি নোংরা। → খালি স্ট্রিং রাখুন, প্রদর্শনে `'—'`।

**L-5 — পেমেন্টের তারিখ মুক্ত টেক্সট ইনপুট।** `admin.html:544` + প্রিফিল `todayBn()` (`admin.html:1334`); ফরম্যাট যাচাই নেই → যেকোনো স্ট্রিং ঢুকতে পারে, যা `analytics().todayCollection`-এর `p.date === todayBn()` তুলনা (`data.js:641`) ভেঙে দেয়। → বাংলা-তারিখ ভ্যালিডেটর বা `type="date"` + অভ্যন্তরীণ নর্মালাইজেশন।

**L-6 — Firebase rules-এ অতি-প্রশস্ত লেখার অনুমতি।** `database.rules.json:47` (`notifications/suggestions`: `role !== 'student'`) এবং কয়েকটি পাথে "যেকোনো authenticated ইউজার লিখতে পারবে" — C-4 ঠিক করার সময় এগুলোও সংকুচিত করুন, নইলে এক শিক্ষক অন্যের ডেটা মুছতে পারবে।

**L-7 — Service worker-এ `cache.put` `waitUntil`-এর বাইরে।** `service-worker.js:79-98` → SW বন্ধ হয়ে গেলে ক্যাশ লেখা বাদ পড়তে পারে; rejection আনহ্যান্ডল্ড।

**L-8 — README নিরাপত্তা সম্পর্কে অতিরঞ্জন করে।** `README.md:110-111` ও `182-183`: *"`database.rules.json` enforces the same boundaries server-side once Firebase is deployed"* — C-4 অনুযায়ী এটি সত্য নয় (roles কখনো লেখা হয় না)। বাকি তথ্য যাচাইকৃত ও নির্ভুল: ২৪টি পারমিশন ✅, ১৭টি রিপোর্ট ✅, গঠন বর্ণনা ✅। → ক্লাউড মোডের প্রকৃত অবস্থা লিখুন।

**L-9 — অফলাইন লেখা কিউ নেই।** `js/app.js:242-249` (`requireOnline`) — Firebase কনফিগারড অবস্থায় অফলাইন হলে লেখা **প্রত্যাখ্যান** করা হয় (লোকাল স্টোরে সেভ করে পরে সিঙ্ক নয়)। `syncState.pending` কাঠামো থাকলেও কিউ নেই। → অফলাইন লেখা কিউ + সংযোগ ফিরলে ফ্লাশ।

---

# ✅ যা ভালো করা হয়েছে (রক্ষা করুন)

| # | বিষয় | প্রমাণ |
|---|---|---|
| P-1 | **XSS-নিরাপদ রেন্ডারিং** — হিউরিস্টিক স্ক্যানে প্রধান সব রেন্ডারারে (`admin-modules.js`, `teacher-home.js`, `student-home.js`, `crud.js`) ব্যবহারকারী-ডেটা ধারাবাহিকভাবে `escapeHtml` হয়েছে; আনএসকেপড `${}` ভেক্টর পাওয়া যায়নি (ব্যতিক্রম H-7) | ম্যানুয়াল + স্ক্রিপ্টেড স্ক্যান |
| P-2 | **ওপেন রিডাইরেক্ট নেই** — `index.html:176-179` `safeNext()` হোয়াইটলিস্ট (`ALLOWED_PAGES`) | কোড রিভিউ |
| P-3 | **নির্ভরযোগ্য সিডিং** — সংগ্রহস্থল খালি না হলে সিড হয় না, ইউনিক ID গার্ড (`nextStudentId` + collision check, `admin.html` সাবমিট), ইমপোর্টে অ্যাপ-নাম ও আকৃতি যাচাই (`data.js:656-657`) | কোড রিভিউ |
| P-4 | **ফর্ম ভ্যালিডেশন সচেতনতা** — `novalidate` + কাস্টম বাংলা এরর, `input-invalid` ক্লাস, প্রতি-ফিল্ড বার্তা (`admin.html` settings/admission) | কোড রিভিউ |
| P-5 | **অ্যাক্টিভিটি লগ** — গুরুত্বপূর্ণ প্রতিটি লেখা `activityLogs`-এ নাম/ভূমিকা/তারিখসহ (`saveOrgInfo` `data.js:394`) — অডিট ট্রেইলের ভালো ভিত্তি | কোড রিভিউ |
| P-6 | **১৬৪টি টেস্ট, jsdom-এ আসল বুট** — পোর্টালের ইনলাইন মডিউল সত্যিই চালিয়ে দেখা হয়; async-এর জন্য `waitFor()` পোলিং (ফ্ল্যাকি স্লিপ নয়) | `npm test` |
| P-7 | **প্রিন্ট/ডকুমেন্ট আলাদাকরণ** — প্রিন্টে অ্যাপ-শেল বাদ (`body.modal-open`), প্রতিষ্ঠানের লেটারহেড, রশিদ/রিপোর্ট টেমপ্লেটে অ্যাপ-UI স্ট্রিং নিষিদ্ধ (টেস্টেও গার্ড) | `js/docs.js`, `tests/docs.test.mjs` |
| P-8 | **PWA বেসিক সঠিক** — সব পেজে `lang="bn"`, viewport, theme-color, apple meta; manifest সঠিক; অফলাইন ফলব্যাক (`caches.match('index.html')`) | ম্যানুয়াল চেক |
| P-9 | **মোটামুটি a11y ভিত্তি** — `prefers-reduced-motion`, `:focus-visible`, `.sr-only`, টোস্টে `aria-live`, টেবিলে `th scope="col"`, skip-link-যোগ্য গঠন | `css/style.css`, `js/app.js` |
| P-10 | **পেজিনেশন সচেতনতা** — বড় তালিকা ২৫/৫০ রো করে দেখানো হয় (spec 62 কমেন্টসহ), যা DOM বোঝা কমায় | `js/app.js:124`, `admin-modules.js:188` |
| P-11 | **বাংলা লোকালেশন গভীর** — সংখ্যা, তারিখ, মুদ্রা, স্ট্যাটাস টার্ম — সবই বাংলায়; হেল্পারগুলো (যদিও ডুপ্লিকেট, M-13) সঠিক | সর্বত্র |
| P-12 | **কোনো অচেনা নির্ভরতা নেই** — রানটাইমে শূন্য npm প্যাকেজ; একমাত্র devDependency jsdom → সাপ্লাই-চেইন ঝুঁকি ও বিল্ড জটিলতা কম | `package.json` |

---

# 🛠 অগ্রাধিকারভিত্তিক রোডম্যাপ

### পর্যায় ১ — এখনই (১–২ দিন): ভুল সংখ্যা ও নীরব ডেটা ক্ষতি বন্ধ
1. **H-2** আংশিক পেমেন্ট যাচাই + `receivePayment` একক-কল রিফ্যাক্টর।
2. **H-3** `storeSet`/`save()` সফলতা রিটার্ন করুক; ব্যর্থতায় লাল টোস্ট।
3. **H-4** `load()` memoize + কালেকশন ইনডেক্স (৩০ মিনিটের কাজ, সবচেয়ে বড় পারফরম্যান্স জয়)।
4. **H-8** `monthlyCollection` মাস-সীমিত, `pendingAssignments` সংজ্ঞা ঠিক, শূন্য-ভাগ গার্ড।
5. **C-4(৩)** `pushRemote` থেকে `.catch(() => null)` সরান — মিথ্যা "synced" বন্ধ।
6. **H-5(১)** `scoreExam`-এ null/খালি উত্তর গার্ড।
7. **H-9** নোটিশ ফিল্টার হেল্পার।

### পর্যায় ২ — এই স্প্রিন্টে (৩–৫ দিন): ফিচার গ্যাপ ও ব্যাকআপ নিরাপত্তা
8. **H-1** বকেয়া তৈরি UI + মাসিক বিলিং + `admissionFee` ইনভয়েস।
9. **H-6** `importBackup` সম্পূর্ণ কী গ্রহণ + `DATA_VERSION` মাইগ্রেশন।
10. **H-7** URL প্রোটোকল হোয়াইটলিস্ট + CSP (চার পেজে)।
11. **M-1/M-2/M-3/M-4** ডেটা-মডেল ও সেটিংস পরিষ্কার (guardianPhone, discount, leaderboard কী, অচল সেটিংস)।
12. **M-6** ৩টি ফেল করা টেস্ট ঠিক + GitHub Actions CI।
13. **M-5/M-14** মৃত সিড ও অ্যাসেট ওজন কমানো (প্রিক্যাশ ~১ MB-এ নামানো সম্ভব)।

### পর্যায় ৩ — পরিকল্পিত (১–২ সপ্তাহ): আর্কিটেকচার ও মান
14. **C-1/C-2/C-3 + C-4** বাস্তব ব্যাকএন্ড/সঠিক Firebase (rules + roles বুটস্ট্র্যাপ + per-collection সিঙ্ক + সব পোর্টালে subscribe) — অথবা স্পষ্টভাবে "একক-ডিভাইস অ্যাপ" হিসেবে স্কোপ ঘোষণা করে নিরাপত্তা দাবি প্রত্যাহার।
15. **M-13** ইনলাইন মডিউল → আলাদা ফাইল; শেয়ার্ড `format.js`; ESLint/Prettier; LICENSE।
16. **M-7/M-8/M-9** অ্যাক্সেসিবিলিটি পাস (মোডাল ফোকাস, লেবেল, কনট্রাস্ট টোকেন)।
17. **M-11/M-15** ভেক্টর PDF ও সেলফ-হোস্টেড ফন্ট।
18. **L-1/L-3/L-9** কভারেজ, debounce, অফলাইন লেখা কিউ।

---

# পরিশিষ্ট

## ক. পদ্ধতি ও টুল

- **ম্যানুয়াল রিভিউ:** `js/` এর ১২টি মডিউল, ৪টি HTML পেজ, ৩টি CSS, `service-worker.js`, `manifest.json`, `database.rules.json`, `tools/`, `tests/`।
- **স্ট্যাটিক স্ক্যান (স্ক্রিপ্ট লিখে চালানো):** আনএসকেপড টেমপ্লেট ইন্টারপোলেশন (XSS হিউরিস্টিক), `javascript:` URI, অ্যাক্সেসিবল-নামবিহীন ফর্ম কনট্রোল (লেবেল/`for`/`aria-label`/wrapping-label বিবেচনাসহ), TODO/FIXME/`console.log`, ডুপ্লিকেট হেল্পার, কল-সাইট কাউন্ট (`db.fees.add` = ০), অচল সেটিংস (প্রতিটি কী-এর জন্য কনজিউমার grep), স্পেক রেফারেন্স কাউন্ট।
- **গতিশীল যাচাই:** `npm test` (১৬৪ টেস্ট, ১৬১ পাস); jsdom বেঞ্চমার্ক হার্নেস লিখে ২০০–২ ০০০ শিক্ষার্থীতে parse/stringify কাউন্ট, ইনসার্ট লেটেন্সি, স্টোর আকার মাপা (বেঞ্চ ফাইলগুলো মুছে ফেলা হয়েছে, রিপোতে রাখা হয়নি); WCAG ২.x কনট্রাস্ট অনুপাত হিসাব (sRGB relative luminance); PNG IHDR থেকে আইকনের প্রকৃত মাত্রা; `md5sum` দিয়ে ডুপ্লিকেট অ্যাসেট শনাক্তকরণ; PRECACHE ওজনের হিসাব।
- **যা করা হয়নি:** নেটওয়ার্ক/পেনিট্রেশন টেস্ট, ব্রাউজারে হাতে-চালানো a11y অডিট (স্ক্রিন-রিডার), মোবাইল ডিভাইস টেস্ট, কোনো রানটাইম প্রোফাইলিং (Node/jsdom-সীমিত)।

## খ. বেঞ্চমার্কের বিস্তারিত (H-4)

```
শিক্ষার্থী   স্টোর      parse/কল   ইনসার্ট      analytics()
২০০        ~৭০ KB      ২         ০.৮৫ ms      ~৫ ms
৩০০        ~১০৫ KB     ১২ (৩৬২ KB রি-পার্স)   ~৮ ms
১ ৫০০      ০.৫৩ MB     —         ৫.২৪ ms      ৩২ ms  (অ্যাডমিন রেন্ডারে ২ বার কল)
২ ০০০      —           —         Node OOM     —
```
প্রতিটি `db.students.add()` = ২× `JSON.parse` + ২× `JSON.stringify` (পুরো স্টোরে)।

## গ. টেস্ট স্ট্যাটাস (M-6)

```
tests/data.test.mjs         ৩৩ পাস / ২ ফেল   → :18 exam window, :21 assignment states
tests/student-home.test.mjs ৪৬ পাস / ১ ফেল   → :30 pending assignment submit
tests/portals-boot.test.mjs ২১ পাস / ০ ফেল
…মোট                      ১৬১ পাস / ৩ ফেল   (রানটাইম ~১৬৪ s)
```
তিনটিই তারিখ-নির্ভর সিডের কারণে (সেপ্টেম্বর ২০২৬-এর হার্ডকোড বাংলা ডেডলাইন পার হয়ে গেছে);
বর্তমান ব্রাঞ্চের পরিবর্তনের **আগে থেকেই** ফেল করছিল (`git stash` করে যাচাইকৃত)।

## ঘ. ফাইল-ভিত্তিক ঝুঁকির চিত্র

| ফাইল | আকার | প্রধান ঝুঁকি |
|---|---|---|
| `js/data.js` | ১ ৩৬১ লাইন / ৬৩.৭ KB | H-1, H-2, H-5, H-6, H-8, M-4, M-5 |
| `js/store.js` | ৪৩ লাইন / ১.৩ KB | H-3 (সবচেয়ে ছোট কিন্তু সবচেয়ে ক্ষতিকর) |
| `js/auth.js` | ৪৩০ লাইন / ১৬.৩ KB | C-1, C-2, C-3 |
| `js/firebase.js` + `database.rules.json` | ৩০৩ লাইন / ১০.১ KB | C-4, L-6 |
| `admin.html` | ১ ৬৬৮ লাইন (৯৭৮ ইনলাইন JS) | H-2, M-1, M-8, M-13 |
| `teacher.html` | ৯৯৯ লাইন (৬১৯ ইনলাইন JS) | H-7, H-9, M-8 |
| `js/admin-modules.js` | ৮১৯ লাইন / ৪৭.৪ KB | H-8, M-2, M-10 |
| `js/student-home.js` | ৮২৭ লাইন / ৫০.১ KB | M-3, H-9 |
| `js/pdf.js` (৬.৬ KB) / `js/docs.js` (৩০.৫ KB) | — | M-11 |
| `js/crud.js` | ১৫৯ লাইন / ৭.১ KB | M-8, M-12, L-3 |
| `service-worker.js` / `assets/` / `tools/` | ৩.১০ MB প্রিক্যাশ | M-14, L-7 |
| `tests/` | ১৬৪ টেস্ট | M-6, L-1 |

---

*এই রিপোর্টের প্রতিটি দাবি ফাইল:লাইন উদ্ধৃতি বা পরিমাপযোগ্য ফলাফল দিয়ে যাচাই করা। কোনো আইটেম নিয়ে প্রশ্ন থাকলে সংশ্লিষ্ট লাইন খুলে দেখুন — সব রেফারেন্স বর্তমান কমিট `5985e9a`-এর বিরুদ্ধে।*
